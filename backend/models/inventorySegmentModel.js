// inventory_segments model removed in simplified workflow. Keep file as a harmless placeholder.
// If you prefer, you can delete this file.

const pool = require('../config/db');

module.exports = {};
